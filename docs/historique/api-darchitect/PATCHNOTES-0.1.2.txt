DimensionArchitect API — PATCHNOTES
mod_version: 0.1.2
minecraft: 1.21.1
loaders: fabric, neoforge
identity: darchitect
generated_utc: 2026-09-19T09:13:05.728546800Z
---

# Unreleased — depuis la dernière mise en ligne

**Depuis la publication :** 0.1.0  
**Cycle commencé :** 2026-09-04  
**mod_version courante :** 0.1.2

Accumule tout ce qui doit figurer dans les notes joueurs / CurseForge au **prochain** cut.  
Ne pas y mettre de noms d’outils / agents.

## Ajouté

- API accès publique : `DimensionArchitect.access()` avec remplacement atomique de la carte des rôles, `clearRole`, et `setAccessMode` / `setOwner` après création (migration soft OPEN → MANAGED).
- Attachments : factory `AttachmentType.of`, suppression `remove`, persistance automatique après `put`/`remove` ; doc branding DA (conteneur géré par l’API, clés `modid:key`).

## Modifié

- Convention nom jar archive : `darchitect-<mod_version>_<loader>_<minecraft>.jar` (ex. `darchitect-0.1.0_fabric_1.21.1.jar`).

## Corrigé

- Crash à la création de dimension sous Fabric (PersonnalWorld / skyblock inclus) : init de `StructureGenerationOps` qui cherchait `getLocateOffset` par nom Yarn ; résolution des champs `StructurePlacement` par type (compatible intermediary / Yarn / Mojmap).
- `setRolesForDimension` avec map vide efface correctement les rôles (plus de no-op).

## Commandes

- Voir [COMMANDS.md](COMMANDS.md) pour le catalogue de la version publiée.

## Limites connues

- Voir [0.1.0.md](0.1.0.md) (limites de la bêta Fabric publiée).
- Unload/load publics API : prévu 0.1.3.
- Couches JSON structurées par mod dans le snapshot : différé.

## Breaking

_(aucun depuis 0.1.0 — ajouts API seulement)_
