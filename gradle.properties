# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true
# OneDrive locke souvent les sorties Gradle (processResources : Failed to clean up stale outputs).
org.gradle.vfs.watch=false

# Mod properties
mod.version=1.3.0-alpha.28
mod.group=fr.galsaxx
mod.id=personnalworld
mod.name=PersonnalWorld

# Shared dependencies
deps.fabric_loader=0.18.4
deps.architectury=13.0.8
deps.mixin_extras=0.4.1
# Optionnel à l’exécution (suggests / optional) — compileOnly + runClient.
# 4.7.5.1 : compatible Loom 1.10 / Fabric API 0.116.0. 4.9.2 ne se remap pas avec ce Loom.
deps.geckolib=4.7.5.1
